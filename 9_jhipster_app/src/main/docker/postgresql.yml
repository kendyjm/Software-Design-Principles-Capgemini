version: '2'
services:
    monolithic_app-postgresql:
        image: postgres:9.6.5
        # volumes:
        #     - ~/volumes/jhipster/monolithic_app/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=monolithic_app
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
